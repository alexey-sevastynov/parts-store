/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    formats: ['image/avif', 'image/webp'],
    domains: ['lh3.googleusercontent.com', 'cdn3.iconfinder.com', 'utfs.io'],
  },
};

export default nextConfig;
